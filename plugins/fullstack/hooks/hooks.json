{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(docker:*)",
        "hooks": [
          {
            "type": "validation",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/check-dockerfile.sh"
          }
        ]
      }
    ]
  }
}
