{
  "name": "base-bun-server",
  "version": "0.0.0",
  "private": true,
  "description": "Bun HTTP service template (Hono + Drizzle + SQLite)",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint",
    "format": "oxfmt",
    "test": "bun test"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "hono": "^4.6.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@infra-x/code-quality": "^0.5.0",
    "@infra-x/typescript-config": "^2.0.0",
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.0",
    "oxfmt": "^0.47.0",
    "oxlint": "^1.0.0",
    "typescript": "^6.0.0"
  }
}
