# Public browser configuration. Restart the dev server after editing .env.local.
VITE_APP_NAME=Vite React
# Optional absolute HTTP(S) API base URL, including a path prefix if needed.
# Leave empty to send requests to the current origin.
VITE_API_URL=
